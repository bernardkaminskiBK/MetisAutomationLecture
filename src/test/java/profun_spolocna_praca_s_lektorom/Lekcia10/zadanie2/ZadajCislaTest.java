/*
package profun_spolocna_praca_s_lektorom.Lekcia10.zadanie2;

import org.junit.jupiter.api.Test;
import utils.StorePrintStream;

import java.util.Scanner;

class ZadajCislaTest {

    @Test
    public void generujStatistikyTest() {
        System.setOut(new StorePrintStream(System.out));

        String input = "";

        for (int i = 1; i < 21; i++) {
            input += i + "\n";
        }

        ZadajCislaTester.zadajCisla(new Scanner(input));

        System.out.println("\n\n\nactual result print: " + StorePrintStream.printList);
    }
}
 */
